import { ClickOutsideNavDirective } from './click-outside-nav.directive';

describe('ClickOutsideNavDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickOutsideNavDirective();
    expect(directive).toBeTruthy();
  });
});
